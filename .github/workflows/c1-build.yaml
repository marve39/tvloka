name: c1-build
on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]
# defaults:
#   run:
#     working-directory: backend
jobs:
  quality:
    name: c1-build
    runs-on: ubuntu-latest
    steps:
    - name: Install Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.16.x
    - name: Install Go-Test
      run:  go get -u golang.org/x/lint/golint && 
            go get -u github.com/axw/gocov/... && 
            go get -u github.com/AlekSi/gocov-xml && 
            go get -u github.com/jstemmer/go-junit-report
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
    - name: Cache SonarCloud packages
      uses: actions/cache@v2
      with:
        path: ~\.sonar\cache
        key: ${{ runner.os }}-sonar
        restore-keys: ${{ runner.os }}-sonar
    - name: run-ci
      working-directory: ./c1
      env: 
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run:  make run-ci && cat coverage.xml
    # - name: fix code coverage paths backend
    #   working-directory: ./backend
    #   run: |
    #     sed -i 's/\/home\/runner\/work\/camp-core-service\/camp-core-service\//\/github\/workspace\//g' coverage/lcov.info
    #     sed -i 's/\/home\/runner\/work\/camp-core-service\/camp-core-service\//\/github\/workspace\//g' test-report.xml
    # - name: fix code coverage paths frontend
    #   working-directory: ./frontend
    #   run: |
    #     sed -i 's/\/home\/runner\/work\/camp-core-service\/camp-core-service\//\/github\/workspace\//g' coverage/lcov.info
    #     sed -i 's/\/home\/runner\/work\/camp-core-service\/camp-core-service\//\/github\/workspace\//g' test-report.xml
    # - name: Analyze with SonarCloud
    #   uses: sonarsource/sonarcloud-github-action@master
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    #   with:
    #     args: >
    #       -Dsonar.host.url=https://sonarcloud.io
    #       -Dsonar.projectKey=centraldigital_camp-core-service
    #       -Dsonar.organization=centraltechnology
    #       -Dsonar.login=${SONAR_TOKEN}